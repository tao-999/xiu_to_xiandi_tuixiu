class HellGameState {
  final int killed;
  final bool bossSpawned;
  final int spawned;

  HellGameState({
    required this.killed,
    required this.bossSpawned,
    required this.spawned,
  });
}
